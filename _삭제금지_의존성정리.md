# ⛔ 이 repo 삭제 금지 — 의존성 정리

> 작성일: 2026-06-01 · 작성: Claude Code (저장소 정리 조사)
> 목적: `sanda-guild-desktop-releases` repo를 **왜 지우면 안 되는지**, 언제·어떻게 정리할 수 있는지 나중에 바로 확인하기 위한 메모.

---

## 한 줄 결론

**산다길드 데스크톱 앱이 길드원에게 배포·사용되는 동안은 이 repo를 절대 삭제하면 안 된다.**
설치된 앱들이 이 repo를 자동 업데이트 서버로 직접 바라보고 있기 때문.

---

## 이 repo의 정체

- **이름**: `NJ-KIM4/sanda-guild-desktop-releases` (public)
- **역할**: 산다길드 데스크톱 앱(Tauri 2)의 **자동 업데이트 매니페스트(`latest.json`) + 인스톨러(`.exe`, `.sig`) 호스팅용 public repo**
- **로컬 폴더 내용**: `README.md` 1개뿐. 실제 산출물은 GitHub **Releases**에 올라가 있음(working tree 아님).
- **왜 public인가**: 데스크톱 앱 소스는 private(`nyanggames`)에 있는데, private repo의 release URL은 인증 없이는 404라 길드원 앱이 못 받음. 그래서 **공개 repo로 따로 분리**해 호스팅.

---

## 전체 의존성 체인 (3개 repo가 묶여 있음)

```
[빌드/CI]  nyanggames (private)
   └ .github/workflows/desktop-release.yml
        · desktop-v* 태그 push 시 동작
        · Tauri 빌드 → 서명 → latest.json URL을 이 repo로 치환
        · RELEASE_REPO_PAT 으로 ↓ 여기에 release 발행
                 │
                 ▼
[배포/호스팅]  sanda-guild-desktop-releases (public, 이 repo)  ⛔
        · Releases 에 .exe / .exe.sig / latest.json 보관
                 │
        ┌────────┴─────────────────────────────┐
        ▼                                       ▼
[설치된 앱]                              [웹 다운로드 페이지]
 sanda-guild-desktop                     sanda-guild (sandakoko.kr, 운영중)
  src-tauri/tauri.conf.json:50            src/app/api/desktop-release/route.ts:4
  updater endpoint =                      REPO = "NJ-KIM4/sanda-guild-desktop-releases"
  .../sanda-guild-desktop-releases/       → GitHub API로 최신 릴리즈 조회해
     releases/latest/download/latest.json   다운로드 링크/버전 표시
```

---

## 삭제하면 깨지는 것 (구체 위치)

| # | 영향 | 근거 파일 |
|---|------|-----------|
| 1 | **설치된 데스크톱 앱 전부 자동 업데이트 영구 중단** (latest.json 404). 출고된 바이너리에 URL이 박혀 있어 사후 변경 불가 | `sanda-guild-desktop/src-tauri/tauri.conf.json:50` |
| 2 | **산다길드 웹(sandakoko.kr) 다운로드/버전 API 실패** (502) | `sanda-guild/src/app/api/desktop-release/route.ts:4` |
| 3 | **릴리즈 발행 CI 파이프라인 끊김** (발행 대상 repo 소멸) | `nyanggames/.github/workflows/desktop-release.yml:113,135,181` |

---

## 동작 원리 요약 (자동 업데이트)

```
앱 시작 5초 후 → updater endpoint(latest.json) GET
 → 현재버전 < latest.json.version 이면 .nsis.zip + .sig 다운로드
 → public key(minisign)로 서명 검증 → NSIS passive 설치 → 앱 재시작
```
- 매니페스트 URL: `https://github.com/NJ-KIM4/sanda-guild-desktop-releases/releases/latest/download/latest.json`
- 상세 운영 가이드: `sanda-guild-desktop/docs/AUTO_UPDATE.md`

---

## 언제 삭제해도 되나 (안전 조건)

다음이 **모두** 충족되면 정리 가능:
1. 산다길드 **데스크톱 앱을 공식 폐기**(더 이상 배포·사용 안 함)하기로 결정
2. 길드원에게 데스크톱 앱 사용 중단/대체 안내 완료 (혹은 잔존 사용자 무시 가능 판단)

> 참고: 이 repo만 따로 지우는 건 의미가 적다. 데스크톱 앱 인프라는 **`nyanggames`(CI/소스) + 이 repo(호스팅) + 앱 엔드포인트**가 한 덩어리. 폐기하려면 같이 정리해야 한다. (`nyanggames`도 같은 이유로 현재 삭제 보류 상태)

---

## 폐기 시 정리 순서 (체크리스트)

- [ ] 1. 데스크톱 앱 폐기 결정 + 길드원 공지
- [ ] 2. 웹 정리: `sanda-guild`의 다운로드 UI + `src/app/api/desktop-release/route.ts` 제거 후 sandakoko.kr 재배포
- [ ] 3. CI 정리: `nyanggames/.github/workflows/desktop-release.yml` 삭제 + 관련 GitHub Secrets(`RELEASE_REPO_PAT`, `TAURI_SIGNING_PRIVATE_KEY` 등) 정리
- [ ] 4. 앱 소스 처리: `sanda-guild-desktop` repo 보관/삭제 결정
- [ ] 5. **이 repo(`sanda-guild-desktop-releases`) GitHub + 로컬 삭제**
- [ ] 6. (선택) 서명 키 `C:\Users\hi_sp\.tauri\sanda-guild-desktop.key` 폐기 — 단 재배포 가능성 있으면 보관

---

## git 상태 (조사 시점 2026-06-01)

- `main` ↔ `origin/main` 완전 동기화, unpushed/uncommitted 없음
- 로컬 손실 위험 없음 (README만 존재). **차단 사유는 순전히 운영 의존성**.
